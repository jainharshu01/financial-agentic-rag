# ============================================================
# Financial Agentic RAG — Runtime Requirements
# Python 3.11 (set via runtime.txt)
# Only what the running app needs — no data-prep tools
# ============================================================

# ---- Core RAG stack ----
chromadb>=0.5.0,<0.6.0
sentence-transformers>=3.0.0,<4.0.0
torch>=2.3.0

# ---- LLM API ----
groq>=0.9.0
httpx==0.27.2

# ---- Web framework ----
streamlit==1.37.0
plotly>=5.22.0

# ---- Data ----
pandas>=2.0.0
numpy>=1.26.0,<2.0.0

# ---- Env ----
python-dotenv>=1.0.0